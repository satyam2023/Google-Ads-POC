import { StyleSheet } from "react-native";

export const commonStyles=StyleSheet.create({
    btnBox:{
        borderWidth:2,
        justifyContent:'center',
        alignItems:'center',
        padding:10,
        borderRadius:10,
        marginHorizontal:20
    }
})